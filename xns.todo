renaming:
	xns renaming, parameter_pack

memory:
	implementer un weak pointer version shared

vector:
	change size to size_type, and rename length method by size

bst_iterator:
	fix const iterator is mutable sometimes...

tree:
	rework signness of node members

make_signed/make_unsigned:
	tests fails with [enum : bool] (need to detect underlying type)
